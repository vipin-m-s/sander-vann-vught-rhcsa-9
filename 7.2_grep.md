  149  ps aux | grep ssh
  150  ps aux | grep ssh | grep -v grep
  151  cd /etc
  152  grep devops *
  153  grep devops *  2>/dev/null
  154  grep -i devops *  2>/dev/null
  155  grep -B 5 Allow /etc/ssh/ssh_config
  156  grep -B 5 allow /etc/ssh/ssh_config
  157  grep -B 5 devops /etc/passwd
  158  grep -B 5 -A 5 devops /etc/passwd
  159  grep -R root
  160  grep -Rl root *
  161  grep -Rl -B 5 root *
  162  grep -R -B 5 root *
  163  grep -R -B 5 -A 5 root *
